#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>
#include <errno.h>
#include <unistd.h>
#include <getopt.h>
#include <stdlib.h>
#include <time.h>
#include <fstream>
#include <cstdlib>
#include <vector>
#include <iostream>
#include <string>
#include <sstream>
#include <unordered_map>

struct send_message {
    uint8_t operation;
    unsigned char key[16];
    unsigned char value[32];
};

struct response_message {
    uint8_t status_code;
    unsigned char key[16];
    unsigned char value[32];
};


int main(int argc, char* argv[])
{
	std::string outputfilename = "output";
	if(argc>0) outputfilename = std::string(argv[0]);
	std::ifstream ofs;
	ofs.open(outputfilename);
	std::string line;
	hashmap = new std::unordered_map<std::string, std::string>();
}
